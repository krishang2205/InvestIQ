{
    "name": "investiq-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && ..\\.venv\\Scripts\\python.exe app.py\"",
        "install:all": "npm install && npm install --prefix frontend && cd backend && ..\\.venv\\Scripts\\pip.exe install -r requirements.txt"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}